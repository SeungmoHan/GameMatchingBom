<UserControl x:Class="Test.UpdateTodayRecordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Test"
             mc:Ignorable="d" 
             Background="LightGray"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="40"></RowDefinition>
        </Grid.RowDefinitions>
        <Button></Button>
        <TextBlock Grid.Column="0" Grid.Row="0" Text="선택되어있는 맴버들..." FontWeight="Bold" FontSize="25"></TextBlock>
        <DataGrid Grid.Column="0"  Grid.Row="1" x:Name="dgSelectedMember"
                  IsReadOnly="True" 
                  SelectionMode="Single">
        </DataGrid>
        <TextBlock Grid.Column="1" Grid.Row="0" Text="소취밤 랭킹" FontWeight="Bold" FontSize="25"></TextBlock>
        <DataGrid Grid.Column="1"  Grid.Row="1" x:Name="dgTodaysMember"
                  PreviewMouseDoubleClick="dgRankInfoViewDoubleClicked"
                  IsReadOnly="True">
        </DataGrid>
        <DockPanel Grid.Row="2">
            <ComboBox x:Name="cbGameTypeChoice" Width="200" IsReadOnly="True"></ComboBox>
            <Button Margin="15,0" Grid.Column="0" Grid.Row="2" Content="업데이트 텍스트 export" 
                    Click="btnUpdateQueryCreate" 
                    HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            </Button>
        </DockPanel>
        <Button Margin="15,0" Grid.Column="1" Grid.Row="2" Content="순위 리로드" 
                Width="200"
                Click="btnReloadData" 
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        </Button>
    </Grid>
</UserControl>
