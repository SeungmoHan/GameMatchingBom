<UserControl x:Class="NewMatchingBom.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hc="https://handyorg.github.io/handycontrol">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Card -->
        <hc:Card Grid.Row="0" Style="{StaticResource ModernCard}" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="🙋‍♂️ 멤버 관리 및 매칭" 
                          Style="{StaticResource TitleText}"
                          Foreground="{StaticResource PrimaryBrush}"/>

                <!-- Search and Load Section -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <hc:SearchBar Grid.Column="0" 
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 hc:InfoElement.Placeholder="멤버 이름 또는 닉네임으로 검색..."
                                 Style="{StaticResource ModernTextBox}"
                                 Height="40"
                                 Margin="0,0,15,0"/>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="☁️ 유저 정보 새로고침" 
                               Command="{Binding LoadFromGoogleSheetsCommand}"
                               Style="{StaticResource SuccessButton}"
                               Height="40"/>
                    </StackPanel>
                </Grid>

                <!-- Matching Configuration -->
                <Border Background="{StaticResource SurfaceBrush}" 
                       CornerRadius="8" 
                       Padding="20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="팀 크기:" 
                                      Style="{StaticResource BodyText}"
                                      VerticalAlignment="Center"
                                      Margin="0,0,10,0"/>
                            <ComboBox Style="{StaticResource ModernComboBox}" 
                                     Width="100" 
                                     Height="35"
                                     IsEnabled="False"
                                     Foreground="Black">
                                <ComboBox.Items>
                                    <ComboBoxItem Content="1명"/>
                                    <ComboBoxItem Content="2명"/>
                                    <ComboBoxItem Content="3명"/>
                                    <ComboBoxItem Content="4명"/>
                                    <ComboBoxItem Content="5명" IsSelected="True"/>
                                    <ComboBoxItem Content="6명"/>
                                    <ComboBoxItem Content="7명"/>
                                    <ComboBoxItem Content="8명"/>
                                    <ComboBoxItem Content="9명"/>
                                    <ComboBoxItem Content="10명"/>
                                </ComboBox.Items>
                            </ComboBox>
                        </StackPanel>

                        <CheckBox Grid.Column="2"
                                 Content="포지션별 매칭" 
                                 IsChecked="{Binding UseLine}"
                                 VerticalAlignment="Center"
                                 Foreground="{StaticResource TextPrimaryBrush}"
                                 Margin="20,0"/>

                        <Button Grid.Column="3"
                               Content="🚀 매칭 시작" 
                               Command="{Binding StartMatchingCommand}"
                               Style="{StaticResource DangerButton}"
                               FontWeight="Bold"
                               Height="40"
                               Padding="30,0"/>
                    </Grid>
                </Border>
            </StackPanel>
        </hc:Card>

        <!-- Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- All Members -->
            <hc:Card Grid.Column="0" Style="{StaticResource ModernCard}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" 
                              Text="👥 전체 멤버" 
                              Style="{StaticResource SubtitleText}"/>

                    <Border Grid.Row="1"
                           Background="{StaticResource SurfaceBrush}"
                           CornerRadius="8"
                           Padding="10">
                        <DataGrid ItemsSource="{Binding FilteredMembers}"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 GridLinesVisibility="None"
                                 HeadersVisibility="Column"
                                 AutoGenerateColumns="False"
                                 CanUserAddRows="False"
                                 CanUserDeleteRows="False"
                                 CanUserReorderColumns="False"
                                 CanUserResizeRows="False"
                                 IsReadOnly="True"
                                 SelectionMode="Single"
                                 RowHeight="50"
                                 AlternatingRowBackground="{StaticResource CardBgBrush}"
                                 RowBackground="Transparent">
                            
                            <!-- DataGrid Styling -->
                            <DataGrid.Resources>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="FontSize" Value="12"/>
                                    <Setter Property="Height" Value="40"/>
                                    <Setter Property="Padding" Value="10,0"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                                    <Setter Property="BorderThickness" Value="0,0,1,0"/>
                                    <Setter Property="BorderBrush" Value="White"/>
                                </Style>
                                
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                                
                                <Style TargetType="DataGridCell">
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Padding" Value="10,5"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.Resources>

                            <DataGrid.Columns>
                                <!-- Name Column -->
                                <DataGridTextColumn Header="이름" 
                                                   Binding="{Binding Name}" 
                                                   Width="*"
                                                   MinWidth="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                            <Setter Property="FontSize" Value="14"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Margin" Value="5,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Tier Column -->
                                <DataGridTemplateColumn Header="티어" Width="80">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="{StaticResource PrimaryBrush}" 
                                                   CornerRadius="12" 
                                                   Padding="8,4"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Tier}" 
                                                          Foreground="White"
                                                          FontSize="10"
                                                          FontWeight="Medium"
                                                          HorizontalAlignment="Center"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <!-- Main Position Column -->
                                <DataGridTemplateColumn Header="주 포지션" Width="100">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="{StaticResource AccentBrush}" 
                                                   CornerRadius="12" 
                                                   Padding="8,4"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center">
                                                <TextBlock Text="{Binding MainLine}" 
                                                          Foreground="White"
                                                          FontSize="10"
                                                          FontWeight="Medium"
                                                          HorizontalAlignment="Center"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <!-- Actions Column -->
                                <DataGridTemplateColumn Header="작업" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" 
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center">
                                                <Button Content="➕" 
                                                       Command="{Binding DataContext.AddToSelectedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                       CommandParameter="{Binding}"
                                                       Style="{StaticResource SuccessButton}"
                                                       Width="28" Height="28"
                                                       FontSize="12"
                                                       Margin="2,0"
                                                       Padding="0"
                                                       ToolTip="매칭 대상에 추가"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </hc:Card>

            <!-- Selected Members -->
            <hc:Card Grid.Column="2" Style="{StaticResource ModernCard}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <TextBlock Text="🎯 매칭 대상" 
                                  Style="{StaticResource SubtitleText}"/>
                        <Border Background="{StaticResource PrimaryBrush}" 
                               CornerRadius="12" 
                               Padding="8,4"
                               VerticalAlignment="Center"
                               Margin="10,0,0,0">
                            <TextBlock Text="{Binding SelectedMembers.Count, StringFormat='{}{0}명'}" 
                                      Foreground="White"
                                      FontWeight="Bold"
                                      FontSize="12"/>
                        </Border>
                    </StackPanel>

                    <Border Grid.Row="1"
                           Background="{StaticResource SurfaceBrush}"
                           CornerRadius="8"
                           Padding="0">
                        <ListBox ItemsSource="{Binding SelectedMembers}"
                                Background="Transparent"
                                BorderThickness="0"
                                ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource SuccessBrush}" 
                                           CornerRadius="8" 
                                           Padding="15,10" 
                                           Margin="5"
                                           Opacity="0.9">
                                        <Border.Effect>
                                            <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="8"/>
                                        </Border.Effect>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding Name}" 
                                                          FontWeight="SemiBold" 
                                                          Foreground="White"
                                                          FontSize="14"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                                                    <Border Background="White" 
                                                           CornerRadius="10" 
                                                           Padding="6,2"
                                                           Opacity="0.9">
                                                        <TextBlock Text="{Binding Tier}" 
                                                                  Foreground="{StaticResource SuccessBrush}"
                                                                  FontSize="10"
                                                                  FontWeight="Medium"/>
                                                    </Border>
                                                    <Border Background="White" 
                                                           CornerRadius="10" 
                                                           Padding="6,2"
                                                           Margin="5,0,0,0"
                                                           Opacity="0.9">
                                                        <TextBlock Text="{Binding MainLine}" 
                                                                  Foreground="{StaticResource SuccessBrush}"
                                                                  FontSize="10"/>
                                                    </Border>
                                                </StackPanel>
                                            </StackPanel>

                                            <Button Grid.Column="1" 
                                                   Content="➖" 
                                                   Command="{Binding DataContext.RemoveFromSelectedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                   CommandParameter="{Binding}"
                                                   Style="{StaticResource WarningButton}"
                                                   Width="32" Height="32"
                                                   FontSize="14"
                                                   Padding="0"
                                                   ToolTip="매칭 대상에서 제거"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Border>

                    <Button Grid.Row="2" 
                           Content="🗑️ 전체 제거" 
                           Command="{Binding ClearSelectedCommand}"
                           Style="{StaticResource WarningButton}"
                           Margin="0,15,0,0"
                           Height="40"/>
                </Grid>
            </hc:Card>
        </Grid>
    </Grid>
</UserControl>