<UserControl x:Class="NewMatchingBom.Views.UpdateRecordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hc="https://handyorg.github.io/handycontrol">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Card -->
        <hc:Card Grid.Row="0" Style="{StaticResource ModernCard}" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="📊 멤버 점수 관리" 
                          Style="{StaticResource TitleText}"
                          Foreground="{StaticResource PrimaryBrush}"/>

                <!-- Add Record Section -->
                <Border Background="{StaticResource SurfaceBrush}" 
                       CornerRadius="8" 
                       Padding="15"
                       Margin="0,0,0,15">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" 
                                  Text="🎯 새 기록 추가" 
                                  Style="{StaticResource SubtitleText}"
                                  Margin="0,0,0,10"/>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Member Selection GridView -->
                            <Border Grid.Column="0" 
                                   Background="{StaticResource CardBgBrush}"
                                   CornerRadius="8"
                                   BorderBrush="{StaticResource SurfaceBrush}"
                                   BorderThickness="1"
                                   MaxHeight="80"
                                   Margin="0,0,15,0">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    
                                    <Border Grid.Row="0" 
                                           Background="{StaticResource AccentBrush}"
                                           CornerRadius="8,8,0,0"
                                           Padding="10,5">
                                        <TextBlock Text="현재 선택된 멤버" 
                                                  Foreground="White"
                                                  FontWeight="SemiBold"
                                                  FontSize="12"/>
                                    </Border>
                                    
                                    <ScrollViewer Grid.Row="1" 
                                                 VerticalScrollBarVisibility="Auto"
                                                 Padding="8">
                                        <ListView ItemsSource="{Binding SelectedMembersForRecord}"
                                                 Background="Transparent"
                                                 BorderThickness="0"
                                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                            <ListView.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel Orientation="Horizontal"/>
                                                </ItemsPanelTemplate>
                                            </ListView.ItemsPanel>
                                            <ListView.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Background="{StaticResource SuccessBrush}" 
                                                           CornerRadius="12" 
                                                           Padding="8,4" 
                                                           Margin="2"
                                                           Cursor="Hand">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock Grid.Column="0" 
                                                                      Text="{Binding Name}" 
                                                                      Foreground="White"
                                                                      FontSize="12"
                                                                      FontWeight="Medium"
                                                                      VerticalAlignment="Center"/>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ListView.ItemTemplate>
                                        </ListView>
                                    </ScrollViewer>
                                </Grid>
                            </Border>

                            <ComboBox Grid.Column="1" 
                                     ItemsSource="{Binding GameTypes}"
                                     SelectedItem="{Binding SelectedGameType}"
                                     Style="{StaticResource ModernComboBox}"
                                     Height="35"
                                     Foreground="Black"
                                     Margin="0,0,15,0"/>

                            <hc:TextBox Grid.Column="2" 
                                       Text="{Binding SelectedDate}"
                                       hc:InfoElement.Placeholder="YYYY-MM-DD"
                                       Style="{StaticResource ModernTextBox}"
                                       Height="35"
                                       Margin="0,0,15,0"/>

                            <Button Grid.Column="3" 
                                   Content="📝 기록 추가" 
                                   Command="{Binding AddRecordCommand}"
                                   Style="{StaticResource SuccessButton}"
                                   Height="35"
                                   Padding="15,0"
                                   FontWeight="Bold"/>
                        </Grid>
                    </Grid>
                </Border>


                <!-- Control Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                    <Button Content="🔄 랭킹 새로고침" 
                           Command="{Binding RefreshRankingCommand}"
                           Style="{StaticResource AccentButton}"
                           Height="38"
                           Padding="15,0"
                           Margin="0,0,10,0"/>
                    <Button Content="📤 랭킹 내보내기" 
                           Command="{Binding ExportRankingCommand}"
                           Style="{StaticResource WarningButton}"
                           Height="38"
                           Padding="15,0"
                           Margin="0,0,10,0"/>
                    <Button Content="☁️ 플레이 정보 새로고침" 
                           Command="{Binding LoadFromGoogleSheetsCommand}"
                           Style="{StaticResource SuccessButton}"
                           Height="38"
                           Padding="15,0"/>
                </StackPanel>
            </StackPanel>
        </hc:Card>

        <!-- Ranking Content -->
        <hc:Card Grid.Row="1" Style="{StaticResource ModernCard}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" 
                          Text="🏆 멤버 랭킹" 
                          Style="{StaticResource SubtitleText}"/>

                <Border Grid.Row="1"
                       Background="{StaticResource SurfaceBrush}"
                       CornerRadius="8"
                       Padding="10"
                       Margin="0,10,0,0">
                    <DataGrid ItemsSource="{Binding RankingData}"
                             AutoGenerateColumns="False"
                             HeadersVisibility="Column"
                             GridLinesVisibility="Horizontal"
                             Background="Transparent"
                             Foreground="{StaticResource TextPrimaryBrush}"
                             BorderThickness="0"
                             RowBackground="{StaticResource CardBgBrush}"
                             AlternatingRowBackground="{StaticResource SurfaceBrush}"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Disabled">
                        
                        <DataGrid.InputBindings>
                            <MouseBinding MouseAction="LeftDoubleClick" 
                                         Command="{Binding ShowMemberDetailsCommand}" 
                                         CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                        </DataGrid.InputBindings>
                        
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Padding" Value="12,8"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="순위" 
                                               Binding="{Binding Rank}" 
                                               Foreground="White"
                                               IsReadOnly="True"
                                               Width="80">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="FontSize" Value="14"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            
                            <DataGridTextColumn Header="이름" 
                                               Binding="{Binding Name}" 
                                               Foreground="White"
                                               IsReadOnly="True"
                                               Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Margin" Value="12,8"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            
                            <DataGridTextColumn Header="총점" 
                                               Binding="{Binding TotalPoint}" 
                                               Foreground="White"
                                               IsReadOnly="True"
                                               Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="FontSize" Value="14"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>

                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="{StaticResource CardBgBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                <Setter Property="MinHeight" Value="45"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Rank}" Value="1">
                                        <Setter Property="Background" Value="#FFD700"/>
                                        <Setter Property="Foreground" Value="Black"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Rank}" Value="2">
                                        <Setter Property="Background" Value="#C0C0C0"/>
                                        <Setter Property="Foreground" Value="Black"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Rank}" Value="3">
                                        <Setter Property="Background" Value="#CD7F32"/>
                                        <Setter Property="Foreground" Value="Black"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                    </DataGrid>
                </Border>
            </Grid>
        </hc:Card>
    </Grid>
</UserControl>